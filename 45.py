# if __name__=="__main__" 

import module1
module1.jugali()

# basically it is used in modules to to print render the function only one time when its name is called instead of printing it again and again 
# if it is called inside the function also 
